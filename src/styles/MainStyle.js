// src/styles/MainStyle

import { StyleSheet } from 'react-native';

const MainStyle = StyleSheet.create({
  p: {
     color: 'blue',
     fontFamily: 'Open Sans',
     fontSize: 20,
   }
});

export default MainStyle;
